name: First-workflow

on:
    push

jobs:
    hello:
        runs-on: ubuntu-latest

        steps:
          - run: ls-la
          - run: pwd
          - uses: actions\checkout@v4
          - run: pwd
          - run: ls-la